<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pge="clr-namespace:LagoVista.XPlat.Core;assembly=LagoVista.XPlat.Core"
             x:Class="LagoVista.Uas.BaseStation.Controls.Waypoint">
        
    <pge:Label Text="{Binding Command}" Grid.Row="0" />
    <pge:Label Text="{Binding X}" />
    <pge:Label Text="{Binding Y}" />
    <pge:Label Text="{Binding Z}" />
    <Editor Text="{Binding Param1}" />
    <Editor Text="{Binding Param2}" />
    <Editor Text="{Binding Param3}" />
    <Editor Text="{Binding Param4}" />
</StackLayout>